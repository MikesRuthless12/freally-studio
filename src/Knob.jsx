// Knob — consolidated into the component kit (TASK-C05).
// This file remains so existing imports keep working; the implementation
// is the flat, token-colored kit knob.
export { default } from './components/ui/Knob.jsx';
